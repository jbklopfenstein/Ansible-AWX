- hosts: 127.0.0.1
  name: Local Testing no Roles
  become: yes
  vars:
    chgnumber: CHG0043054
  tasks:
    - name: test task
      block:
        - name: test message
          shell: echo "supy supy!" >> /testlog.txt
          notify: update SNOW with success
        - debug:
            msg: "task1 is a success!"
      rescue:
        - name: error handle
          shell: echo "fail saucers" >> /testlog.txt

  handlers:
    - name: update SNOW with success
      uri:
        url: https://ciscodev.service-now.com/api/now/table/change_request?sysparm_limit=1&number={{ chgnumber }}
        method: GET
        user: ansibleAwx
        password: snowpassword
        force_basic_auth: yes
        follow_redirects: all
        status_code: 200
